# מדריך ולידציות במערכת - Content Studio

## 📋 תוכן עניינים
1. [Course Validations](#course-validations)
2. [Topic Validations](#topic-validations)
3. [Content Validations](#content-validations)
4. [Template Validations](#template-validations)
5. [Exercise Validations](#exercise-validations)
6. [DTO Validations](#dto-validations)

---

## 🎓 Course Validations

**קובץ:** `backend/src/domain/entities/Course.js`

### ✅ Validation Rules:

#### 1. **course_name** (שם הקורס)
- **חובה:** ✅ כן
- **טווח אורך:** 3-255 תווים
- **שגיאה:** `"Course name must be between 3 and 255 characters"`
- **למה:** שם הקורס חייב להיות ברור ומזהה, לא קצר מדי ולא ארוך מדי

#### 2. **trainer_id** (מזהה מאמן)
- **חובה:** ✅ כן
- **שגיאה:** `"Trainer ID is required"`
- **למה:** כל קורס חייב להיות משויך למאמן שיצר אותו

#### 3. **description** (תיאור)
- **חובה:** ❌ לא (אופציונלי)
- **מקסימום אורך:** 2000 תווים
- **שגיאה:** `"Description must not exceed 2000 characters"`
- **למה:** תיאור יכול להיות ארוך אבל לא יותר מדי

#### 4. **skills** (כישורים)
- **חובה:** ❌ לא
- **טיפוס:** מערך (Array)
- **ברירת מחדל:** `[]` (מערך ריק)
- **למה:** כישורים הם אופציונליים, אבל אם יש - חייב להיות מערך

#### 5. **language** (שפה)
- **חובה:** ❌ לא
- **ברירת מחדל:** `'en'` (אנגלית)
- **למה:** אם לא צוין, הקורס יהיה באנגלית

#### 6. **status** (סטטוס)
- **חובה:** ❌ לא
- **ברירת מחדל:** `'active'`
- **ערכים אפשריים:** `'active'`, `'archived'`, `'deleted'`
- **למה:** קורס חדש תמיד מתחיל כ-active

---

## 📚 Topic Validations

**קובץ:** `backend/src/domain/entities/Topic.js`

### ✅ Validation Rules:

#### 1. **topic_name** (שם השיעור)
- **חובה:** ✅ כן
- **טווח אורך:** 3-255 תווים
- **שגיאה:** `"Topic name must be between 3 and 255 characters"`
- **למה:** שם השיעור חייב להיות ברור ומזהה

#### 2. **trainer_id** (מזהה מאמן)
- **חובה:** ✅ כן
- **שגיאה:** `"Trainer ID is required"`
- **למה:** כל שיעור חייב להיות משויך למאמן

#### 3. **course_id** (מזהה קורס)
- **חובה:** ❌ לא (אופציונלי)
- **טיפוס:** `number` או `null`
- **למה:** שיעור יכול להיות חלק מקורס או standalone (עצמאי)

#### 4. **language** (שפה) - ⚠️ חשוב!
- **חובה:** ✅ כן **רק אם** `course_id === null` (standalone topic)
- **שגיאה:** `"Language is required for stand-alone topics"`
- **טיפוס:** string, מקסימום 10 תווים
- **שגיאה נוספת:** `"Language must be a string with maximum 10 characters"`
- **למה:** 
  - אם שיעור הוא חלק מקורס → השפה נלקחת מהקורס
  - אם שיעור הוא standalone → חייב לציין שפה

#### 5. **skills** (כישורים)
- **חובה:** ❌ לא
- **טיפוס:** מערך (Array)
- **ברירת מחדל:** `[]`

---

## 📝 Content Validations

**קובץ:** `backend/src/domain/entities/Content.js`

### ✅ Validation Rules:

#### 1. **topic_id** (מזהה שיעור)
- **חובה:** ✅ כן
- **טיפוס:** מספר חיובי (positive integer)
- **שגיאה:** `"topic_id must be a positive integer"`
- **למה:** כל תוכן חייב להיות משויך לשיעור

#### 2. **content_type_id** (סוג תוכן)
- **חובה:** ✅ כן
- **טיפוס:** string או number
- **ערכים אפשריים:**
  - `1` או `'text'` - תוכן טקסטואלי
  - `2` או `'code'` - קוד
  - `3` או `'presentation'` - מצגת
  - `4` או `'audio'` - אודיו
  - `5` או `'mind_map'` - מפת חשיבה
  - `6` או `'avatar_video'` - סרטון אווטר
- **שגיאה:** `"content_type_id is required"`
- **למה:** חייב לדעת איזה סוג תוכן זה

#### 3. **content_data** (נתוני התוכן)
- **חובה:** ✅ כן
- **טיפוס:** אובייקט (Object)
- **שגיאה:** `"content_data must be an object"`
- **למה:** התוכן עצמו חייב להיות אובייקט (JSON)

#### 4. **generation_method_id** (שיטת יצירה)
- **חובה:** ✅ כן
- **טיפוס:** string או number
- **ערכים אפשריים:**
  - `'manual'` - יצירה ידנית
  - `'ai'` - יצירה עם AI
  - `'manual_edited'` - עריכה ידנית
- **שגיאה:** `"generation_method_id is required"`
- **למה:** חייב לדעת איך התוכן נוצר

#### 5. **quality_check_status** (סטטוס בדיקת איכות)
- **חובה:** ❌ לא (אופציונלי)
- **ערכים אפשריים:** `'pending'`, `'approved'`, `'rejected'`, `'needs_revision'`
- **שגיאה:** `"quality_check_status must be one of: pending, approved, rejected, needs_revision"`
- **למה:** אם יש סטטוס, הוא חייב להיות אחד מהערכים המותרים

---

## 🎨 Template Validations

**קובץ:** `backend/src/domain/entities/Template.js`

### ✅ Validation Rules:

#### 1. **template_name** (שם התבנית)
- **חובה:** ✅ כן
- **לא יכול להיות ריק:** ✅ כן (לא רק רווחים)
- **שגיאה:** `"template_name is required and cannot be empty"`
- **למה:** תבנית חייבת שם מזהה

#### 2. **template_type** (סוג תבנית)
- **חובה:** ✅ כן
- **ערכים אפשריים:**
  - `'ready_template'` - תבנית מוכנה
  - `'ai_generated'` - תבנית שנוצרה עם AI
  - `'manual'` - תבנית ידנית
  - `'mixed_ai_manual'` - תבנית מעורבת
- **שגיאה:** `"template_type must be one of: ready_template, ai_generated, manual, mixed_ai_manual"`
- **למה:** חייב לדעת איזה סוג תבנית זה

#### 3. **format_order** (סדר הפורמטים) - ⚠️ חשוב מאוד!

##### 3.1. **חייב להיות מערך:**
- **שגיאה:** `"format_order must be an array"`

##### 3.2. **לא יכול להיות ריק:**
- **שגיאה:** `"format_order cannot be empty"`

##### 3.3. **ערכים מותרים:**
- `'avatar_video'` - סרטון אווטר (אופציונלי)
- `'text'` - טקסט (חובה)
- `'code'` - קוד (חובה)
- `'presentation'` - מצגת (חובה)
- `'audio'` - אודיו (חובה)
- `'mind_map'` - מפת חשיבה (חובה)
- **שגיאה:** `"Invalid content types in format_order: ..."`

##### 3.4. **חייב לכלול את כל 5 הפורמטים החובה:**
- **פורמטים חובה:** `['text', 'code', 'presentation', 'audio', 'mind_map']`
- **שגיאה:** `"Template must include all 5 mandatory formats. Missing: ..."`
- **למה:** כל תבנית חייבת לכלול את כל הפורמטים הנדרשים

##### 3.5. **Audio חייב להיות עם Text:**
- **כלל:** Audio חייב להופיע לפני Text או מיד אחרי Text
- **שגיאה:** `"Audio format must always be with text. Text must appear before audio or immediately after it in the format order."`
- **למה:** אודיו תמיד צריך טקסט לידו (לפני או אחרי)

#### 4. **created_by** (נוצר על ידי)
- **חובה:** ✅ כן
- **לא יכול להיות ריק:** ✅ כן (לא רק רווחים)
- **שגיאה:** `"created_by is required"`
- **למה:** חייב לדעת מי יצר את התבנית

---

## 💪 Exercise Validations

**קובץ:** `backend/src/domain/entities/Exercise.js`

### ✅ Validation Rules:

**הערה:** Exercise Entity לא מכיל ולידציות מפורשות ב-constructor, אבל יש שדות חובה:

#### 1. **topic_id** (מזהה שיעור)
- **חובה:** ✅ כן (מהשימוש במערכת)
- **למה:** כל תרגיל חייב להיות משויך לשיעור

#### 2. **question_text** (טקסט השאלה)
- **חובה:** ✅ כן (מהשימוש במערכת)
- **למה:** תרגיל חייב שאלה

#### 3. **question_type** (סוג שאלה)
- **חובה:** ✅ כן
- **ערכים אפשריים:** `'code'` או `'theoretical'`
- **למה:** חייב לדעת אם זה תרגיל קוד או תיאורטי

#### 4. **programming_language** (שפת תכנות)
- **חובה:** ✅ כן (מהשימוש במערכת)
- **למה:** תרגיל קוד חייב שפת תכנות

#### 5. **language** (שפה)
- **חובה:** ❌ לא
- **ברירת מחדל:** `'en'`
- **למה:** אם לא צוין, התרגיל באנגלית

#### 6. **validation_status** (סטטוס ולידציה)
- **חובה:** ❌ לא
- **ברירת מחדל:** `'pending'`
- **ערכים אפשריים:** `'pending'`, `'approved'`, `'rejected'`
- **למה:** תרגיל חדש מתחיל כ-pending עד לאישור

---

## 📦 DTO Validations

### CourseDTO (`backend/src/application/dtos/CourseDTO.js`)

#### CreateCourseDTO:
- **course_name:** חובה (ללא ולידציה נוספת - Entity יבדוק)
- **description:** אופציונלי, ברירת מחדל: `null`
- **trainer_id:** חובה
- **skills:** מערך, ברירת מחדל: `[]`
- **language:** ברירת מחדל: `'en'`
- **company_logo:** אופציונלי, ברירת מחדל: `null`

#### UpdateCourseDTO:
- **כל השדות אופציונליים** - רק השדות שנשלחו מתעדכנים
- **skills:** אם נשלח, חייב להיות מערך

---

### TopicDTO (`backend/src/application/dtos/TopicDTO.js`)

#### CreateTopicDTO:
- **topic_name:** חובה
- **description:** אופציונלי, ברירת מחדל: `null`
- **trainer_id:** חובה
- **course_id:** אופציונלי, מנורמל ל-`null` אם ריק/לא תקין
- **template_id:** אופציונלי, ברירת מחדל: `null`
- **skills:** מערך, ברירת מחדל: `[]`
- **language:** 
  - אם `course_id === null` → ברירת מחדל: `'en'`
  - אחרת → `null` (נלקח מהקורס)

#### UpdateTopicDTO:
- **כל השדות אופציונליים**
- **course_id:** מנורמל ל-`null` אם ריק/לא תקין
- **skills:** אם נשלח, חייב להיות מערך

---

## 🔍 סיכום - מה נבדק איפה?

### Entity Validations (ב-`domain/entities/`):
- ✅ **ולידציות עסקיות** - כללים של הדומיין
- ✅ **ולידציות חובה** - שדות חובה
- ✅ **ולידציות טווח** - אורך, טיפוס, ערכים מותרים
- ✅ **ולידציות לוגיקה** - כללים מורכבים (כמו Audio+Text)

### DTO Validations (ב-`application/dtos/`):
- ✅ **ניקוי נתונים** - המרת טיפוסים
- ✅ **ערכי ברירת מחדל** - אם לא נשלח, מה הערך
- ✅ **נרמול** - המרת ערכים (כמו course_id ל-null)

### Use Case Validations:
- ✅ **ולידציות עסקיות מורכבות** - כללים שדורשים גישה ל-DB
- ✅ **ולידציות תלויות הקשר** - בדיקות שדורשות מידע נוסף

---

## ⚠️ שגיאות נפוצות

### 1. "Course name must be between 3 and 255 characters"
- **סיבה:** שם קורס קצר מדי או ארוך מדי
- **פתרון:** ודא שהשם בין 3-255 תווים

### 2. "Language is required for stand-alone topics"
- **סיבה:** יצרת standalone topic (ללא course_id) אבל לא ציינת שפה
- **פתרון:** הוסף `language: 'he'` או `language: 'ar'` וכו'

### 3. "Template must include all 5 mandatory formats"
- **סיבה:** תבנית לא כוללת את כל הפורמטים החובה
- **פתרון:** ודא שיש: `text`, `code`, `presentation`, `audio`, `mind_map`

### 4. "Audio format must always be with text"
- **סיבה:** Audio לא מופיע לפני או מיד אחרי Text
- **פתרון:** שנה את `format_order` כך ש-Text יהיה לפני או מיד אחרי Audio

### 5. "topic_id must be a positive integer"
- **סיבה:** topic_id לא תקין (null, 0, שלילי, לא מספר)
- **פתרון:** ודא שיש topic_id תקין ומספר חיובי

---

## 📝 הערות חשובות

1. **ולידציות ב-Entity הן הקריטיות ביותר** - הן רצות תמיד כשמשתמשים ב-Entity
2. **DTOs מנקים נתונים** - אבל לא בודקים ולידציות מורכבות
3. **Use Cases יכולים להוסיף ולידציות** - שדורשות גישה ל-DB או לוגיקה מורכבת
4. **Repository לא אמור לבדוק ולידציות** - רק לשמור/לקרוא מ-DB

---

**עודכן לאחרונה:** 2025-01-29

