name: Supabase Database Migration

on:
  push:
    branches: [ main ]
    paths:
      - 'database/**'
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy Database Migrations
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      # ✅ Use the official Supabase CLI installer
      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Verify Supabase CLI installed
        run: |
          supabase --version
          which supabase

      - name: Verify Supabase Secrets
        run: |
          if [ -z "${{ secrets.SUPABASE_PROJECT_REF }}" ]; then
            echo "❌ ERROR: SUPABASE_PROJECT_REF is not set in GitHub secrets"
            echo "Please add it in: Settings → Secrets and variables → Actions"
            echo "To find it: Go to Supabase Dashboard → Project Settings → General → Reference ID"
            exit 1
          fi
          if [ -z "${{ secrets.SUPABASE_ACCESS_TOKEN }}" ]; then
            echo "❌ ERROR: SUPABASE_ACCESS_TOKEN is not set in GitHub secrets"
            echo "Please add it in: Settings → Secrets and variables → Actions"
            echo "To get it: Go to https://supabase.com/dashboard/account/tokens"
            exit 1
          fi
          echo "✅ Supabase secrets are configured"

      - name: Link to Supabase Project
        run: |
          supabase link --project-ref "${{ secrets.SUPABASE_PROJECT_REF }}"
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Verify Supabase Connection
        run: supabase db list
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Install PostgreSQL client
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client

      - name: Parse database connection info
        id: parse-db
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          python <<'PY'
          import os, urllib.parse
          url = os.environ['DATABASE_URL']
          parsed = urllib.parse.urlparse(url)
          password = urllib.parse.unquote(parsed.password or '')
          print(f"::add-mask::{password}")
          outputs = {
              'host': parsed.hostname or '',
              'port': parsed.port or 5432,
              'user': parsed.username or '',
              'dbname': (parsed.path or '').lstrip('/') or 'postgres',
              'password': password,
          }
          with open(os.environ['GITHUB_OUTPUT'], 'w') as f:
              for key, value in outputs.items():
                  f.write(f"{key}={value}\n")
          PY

      - name: Resolve Supabase IPv4 Address
        id: resolve-ipv4
        run: |
          set -e
          echo "Resolving IPv4 for ${{ steps.parse-db.outputs.host }}..."
          ipv4=$(dig +short A "${{ steps.parse-db.outputs.host }}" | head -n 1)
          if [ -z "$ipv4" ]; then
            echo "❌ ERROR: Unable to resolve IPv4 address for ${{ steps.parse-db.outputs.host }}"
            exit 1
          fi
          echo "IPv4 address: $ipv4"
          echo "ipv4=$ipv4" >> $GITHUB_OUTPUT

      - name: Diagnose Supabase DNS resolution
        run: |
          set -e
          echo "Resolving ${{ steps.parse-db.outputs.host }}..."
          host "${{ steps.parse-db.outputs.host }}" || true
          dig +short "${{ steps.parse-db.outputs.host }}" || true

      # ✅ Run all SQL files under database/migrations/
      - name: Run Custom Migrations from /database/migrations
        run: |
          set -euo pipefail
          export PGSSLMODE=require
          echo "🚀 Running custom migrations from database/migrations..."
          echo "Testing IPv4 connectivity to $PGHOST:$PGPORT..."
          if ! PGPASSWORD="$PGPASSWORD" psql -4 -h "$PGHOST" -p "$PGPORT" -U "$PGUSER" -d "$PGDATABASE" -c '\conninfo' >/dev/null 2>&1; then
            echo "❌ ERROR: Cannot reach $PGHOST via IPv4. Exiting."
            exit 1
          fi

          for file in database/migrations/*.sql; do
            echo "Executing $file..."
            attempts=0
            until [ "$attempts" -ge 3 ]; do
              if PGPASSWORD="$PGPASSWORD" psql -4 -v ON_ERROR_STOP=1 -h "$PGHOST" -p "$PGPORT" -U "$PGUSER" -d "$PGDATABASE" -f "$file"; then
                echo "✅ Executed $file"
                break
              fi
              attempts=$((attempts+1))
              echo "Attempt $attempts failed, retrying in $((attempts*5)) seconds..."
              sleep $((attempts*5))
            done
            if [ "$attempts" -ge 3 ]; then
              echo "❌ ERROR: Failed to execute $file after $attempts attempts."
              exit 2
            fi
          done

          echo "✅ All migrations executed successfully."
        env:
          PGHOST: ${{ steps.resolve-ipv4.outputs.ipv4 }}
          PGPORT: ${{ steps.parse-db.outputs.port }}
          PGUSER: ${{ steps.parse-db.outputs.user }}
          PGDATABASE: ${{ steps.parse-db.outputs.dbname }}
          PGPASSWORD: ${{ steps.parse-db.outputs.password }}

      - name: Migration Summary
        run: echo "✅ Supabase custom migrations completed successfully!"

      - name: Notify on failure
        if: failure()
        run: echo "❌ Database migration failed! Please check the logs."
